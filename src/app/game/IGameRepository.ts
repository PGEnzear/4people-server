/*
import { Room } from "@prisma/client";
import { EGameAmount } from "@src/types/EGameAmount";
import { PrismaRoomResult } from "@src/types/PrismaResult";

export interface IGameRepository {



    getRoomById(roomId: number): Promise<PrismaRoomResult> | undefined;



    getRoomByUserId(userId: BigInt): Promise<PrismaRoomResult> | undefined;



    createRoom(creatorId: BigInt, amount: EGameAmount): Promise<PrismaRoomResult> | never;



}
*/